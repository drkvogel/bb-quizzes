There are two hidden files, .htaccess and .htpasswd, in this folder, to require authentication to the backend which is now live to the outside world on oqs.ndph.ox.ac.uk.

.htaccess should go in ~/public_html/bbquiz/
.htpasswd should go in ~/

